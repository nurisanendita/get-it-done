<div style="padding: 10px;">
  <h2>Tasks List</h2>

  <% @tasks.each do |task| %>
    <h4><%= task.title %></h4>
    <p><%= task.description %></p>
  <% end %>

  <%= link_to 'Create task', new_task_path, class: 'btn btn-primary' %>
</div>
